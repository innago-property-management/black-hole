{
  "tasks": {
    "dev": "BLACK_HOLE_PORT=8765 deno run --allow-env --allow-net --watch main.ts",
    "test": "deno test --allow-env --allow-net"
  },
  "imports": {
    "@std/expect": "jsr:@std/expect@^1.0.13"
  }
}
